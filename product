// product
const product=(a,b)=>{
    return a*b
}
console.log(product(2,3));


//objecrt
const student={
    name: "Naveen",
    age:23,
    greet:function(){console.log(this.name+' age is'+this.age)}
}
console.log(student.greet());
